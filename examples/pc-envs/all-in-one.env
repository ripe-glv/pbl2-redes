# Roda a topologia completa em um unico PC.
# Este modo usa docker-compose.yml, porque todos os containers ficam na mesma
# rede Docker e devem se comunicar por nomes internos como broker-a e
# drone-base-1. Isso evita timeout de retorno pelo IP do proprio host.

COMPOSE_FILE=docker-compose.yml
